import React from 'react'

const Loading = () => {
    return (
        <div style={styles.loadingStyles}>
            Loading....
        </div>
    )
}

const styles = {
    loadingStyles: {
        color: "white",
        fontSize: "48px"
    }
}

export default Loading
