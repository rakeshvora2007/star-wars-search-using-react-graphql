import React from 'react'

const Loading = () => {
    return (
        <div style={styles.loadingStyles}>
            Loading....
        </div>
    )
}

const styles = {
    loadingStyles: {
        color: "white",
        fontSize: "60px",
        margin: "300px auto"
    }
}

export default Loading
